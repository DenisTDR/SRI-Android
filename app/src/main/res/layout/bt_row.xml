<TableLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"

    android:stretchColumns="0,1,2"
    android:paddingTop="0dip" android:layout_gravity="top"
    >
    <TableRow>

        <RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:tools="http://schemas.android.com/tools"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingLeft="7dp"
            android:paddingStart="7dp"
            android:paddingRight="15dp"
            android:paddingEnd="15dp"
            android:paddingTop="1dp"
            android:paddingBottom="1dp"
            android:id="@+id/relativeLayout"
            >
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Young#2"
                android:id="@+id/nameTextView"
                android:textColor="#111"
                android:textSize="17sp"
                />
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="4C:3C:16:25:83:BD"
                android:id="@+id/addressTextView"
                android:layout_below="@+id/nameTextView"
                android:layout_alignParentLeft="true"
                android:layout_alignParentStart="true"
                android:textColor="#777"
                android:textSize="13sp" />



                <ImageView
                    android:layout_height="match_parent"
                    android:layout_width="30dp"
                    android:scaleType="centerInside"
                    android:src="@drawable/available"
                    android:contentDescription="Available device"
                    android:id="@+id/availableImageView"
                    android:layout_alignParentRight="true"
                    android:layout_alignParentEnd="true" />
                <ImageView
                    android:layout_height="match_parent"
                    android:layout_width="30dp"
                    android:scaleType="centerInside"
                    android:src="@drawable/paired"
                    android:contentDescription="Paired device"
                    android:id="@+id/pairedImageView"
                    android:layout_alignParentRight="true"
                    android:layout_alignParentEnd="true"
                    android:layout_marginRight="31dp"
                    android:layout_marginEnd="31dp"/>

        </RelativeLayout>
    </TableRow>
</TableLayout>